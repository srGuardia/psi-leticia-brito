---
import {
  Footer,
  Hero,
  SectionAbout,
  SectionInfo,
  SectionPhrase,
  SectionServices,
} from "@/components";
import Layout from "@/layouts/Layout.astro";
---

<Layout>
  <Hero client:visible />
  <SectionInfo client:visible />
  <SectionServices client:visible />
  <SectionAbout client:visible />
  <SectionPhrase client:visible />
  <Footer client:visible />
</Layout>
