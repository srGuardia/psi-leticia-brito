services:
  psi-leticia-brito:
    container_name: psi-leticia-brito
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=production